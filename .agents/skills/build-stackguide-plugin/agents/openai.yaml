interface:
  display_name: "Build StackGuide Plugin"
  short_description: "Implement bounded local plugin slices"
  default_prompt: "Use $build-stackguide-plugin to implement an accepted local plugin task with scoped tests and no remote activation."
