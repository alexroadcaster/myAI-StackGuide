interface:
  display_name: "Design Context Contracts"
  short_description: "Design context, session and localization contracts"
  default_prompt: "Use $design-context-contracts for the assigned scope: design context, session and localization contracts. Apply the accepted local/deferred contracts and report evidence limits."
