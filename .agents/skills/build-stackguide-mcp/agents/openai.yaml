interface:
  display_name: "Build StackGuide MCP"
  short_description: "Build separately accepted CP-12 mock backend slices"
  default_prompt: "Use $build-stackguide-mcp for the assigned scope: build separately accepted cp-12 mock backend slices. Apply the accepted local/deferred contracts and report evidence limits."
