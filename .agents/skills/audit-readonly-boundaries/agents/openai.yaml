interface:
  display_name: "Audit Read-Only Boundaries"
  short_description: "Audit bounded reads and private/public data separation"
  default_prompt: "Use $audit-readonly-boundaries for the assigned scope: audit bounded reads and private/public data separation. Apply the accepted local/deferred contracts and report evidence limits."
