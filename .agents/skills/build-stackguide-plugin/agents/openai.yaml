interface:
  display_name: "Build StackGuide Plugin"
  short_description: "Build local state, retrieval and bilingual session HTML"
  default_prompt: "Use $build-stackguide-plugin for the assigned scope: build local state, retrieval and bilingual session html. Apply the accepted local/deferred contracts and report evidence limits."
