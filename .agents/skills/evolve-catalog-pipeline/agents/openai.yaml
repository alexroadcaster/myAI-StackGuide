interface:
  display_name: "Evolve Catalog Pipeline"
  short_description: "Build source-owned public catalog and SQLite assets"
  default_prompt: "Use $evolve-catalog-pipeline for the assigned scope: build source-owned public catalog and sqlite assets. Apply the accepted local/deferred contracts and report evidence limits."
