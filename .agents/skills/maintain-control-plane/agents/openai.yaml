interface:
  display_name: "Maintain Control Plane"
  short_description: "Repair material control-plane drift"
  default_prompt: "Use $maintain-control-plane only for material drift in active status, ownership, dependencies, acceptance or evidence claims. Load the smallest affected sections, preserve historical records and report evidence limits."
