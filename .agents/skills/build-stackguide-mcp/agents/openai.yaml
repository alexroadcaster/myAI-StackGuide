interface:
  display_name: "Build StackGuide MCP"
  short_description: "Implement the bounded MCP ledger contract"
  default_prompt: "Use $build-stackguide-mcp to implement an accepted local/mock backend slice with permission and replay tests."
