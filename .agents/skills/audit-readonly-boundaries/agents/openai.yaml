interface:
  display_name: "Audit Read-Only Boundaries"
  short_description: "Review scanner and GitHub permission safety"
  default_prompt: "Use $audit-readonly-boundaries to review read-only scanning or GitHub integration boundaries."
